export class AuthService {
    public isAuthenticated(username: string, password: string): boolean {
      // In un'applicazione reale, qui avresti la logica per verificare le credenziali
      // contro un database o un altro servizio.
      return true;
    }
  }